base:
  <<: *file:$DIR/base.yaml
  interpolated_addition: ${2+2}
  file_stem: ${"$FILE_STEM"}

loaded_base: ${load('$DIR/base.yaml')}


string: ${2+2}

floatstr: "$(str('float'))"

tag_interp: !$(str('float')) ${2+2}

