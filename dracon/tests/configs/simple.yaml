base: &base
  setting1: baseval
  setting2: baseval2

config:
  setting1: newval
  << :
    setting2: newval2
    setting1: newval1
  a_list:
    - item1
    - item2
    - item3

# todo, use !type[inner_t] syntax with a custom yaml builder instead of <>
